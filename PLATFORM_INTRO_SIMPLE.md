# CaseMind - AI 驱动的智能测试中枢

## 🚀 核心逻辑：从“文档”到“用例”的自动化闭环

**痛点**：需求文档多且杂，人工梳理耗时；用例编写重复劳动，覆盖率难保证。
**方案**：通过 **RAG + LLM** 技术，实现需求理解、知识沉淀与用例生成的全链路自动化。

---

## 🏗️ 模块联动与功能亮点

### 1. 目录管理 (Folders) —— 数据入口
*   **关联**：为系统提供原始需求素材（Word/PDF/Markdown）。
*   **亮点**：**增量检测**。自动识别文件变更（新增/修改/删除），只处理变动部分，二次构建速度提升 80%。

### 2. 记忆面板 (Memory) —— 智能大脑
*   **关联**：接收目录管理的文档，输出结构化的 `memory.md` 和知识点（KP）。
*   **亮点**：**AI 压缩理解**。将数百页文档提炼为核心业务逻辑与规则，形成可复用的“系统记忆”。支持手动修正，确保 AI 理解的准确性。

### 3. AI 对话 (Chat) —— 能力出口
*   **关联**：调用“记忆面板”的知识库，根据用户指令执行任务。
*   **亮点**：**多模态生成**。
    *   **问答**：基于事实的精准检索，拒绝幻觉。
    *   **用例生成**：一键产出标准化 JSON/Excel 用例，覆盖正常/异常/边界场景。
    *   **XMind 脑图**：自动生成层级化功能视图，辅助评审。

### 4. 历史资产 (Legacy) —— 风格进化
*   **关联**：作为“参考系”注入 AI 对话环节。
*   **亮点**：**风格学习**。上传团队历史 Excel/XMind，AI 自动学习列映射与编写习惯，使新生成的用例更符合团队规范。

---

## 💡 核心价值
*   **效率革命**：需求分析从“天”级缩短至“分钟”级。
*   **质量闭环**：通过结构化知识库（KP）确保用例不遗漏、可追溯。
*   **安全可控**：纯本地运行，敏感业务数据不出域。

**【图示建议】**
1. **整体架构图**：展示 Folders → Memory → Chat/Legacy 的数据流向。
2. **用例生成对比**：左侧为原始需求片段，右侧为 AI 生成的结构化用例表格。
